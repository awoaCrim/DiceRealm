{
  "name": "dnd-ai-dm-mvp",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"npm run dev --workspace server\" \"npm run dev:client\"",
    "dev:client": "tsx scripts/wait-for-dev-server.ts && npm run dev --workspace client",
    "build": "npm run build --workspace server && npm run build --workspace client",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:phase4-browser": "tsx scripts/phase4-browser-validation.ts",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace client"
  },
  "workspaces": [
    "packages/*",
    "server",
    "client"
  ],
  "engines": {
    "node": ">=24.11.1 <25"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "latest",
    "@testing-library/react": "latest",
    "@testing-library/user-event": "latest",
    "@types/node": "latest",
    "concurrently": "latest",
    "jsdom": "28.1.0",
    "tsx": "^4.22.3",
    "typescript": "latest",
    "vitest": "latest"
  },
  "dependencies": {
    "playwright": "^1.54.1"
  }
}
