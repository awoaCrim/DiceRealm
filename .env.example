# 服务端环境变量示例（仅占位，不包含任何真实密钥）。
# 手动配置请复制到 server/.env（npm --workspace server 的工作目录是 server/，dotenv 从该目录读取），
# 并把 AI_PROVIDER_API_KEY 替换为真实密钥。.env 已在 .gitignore 中忽略，绝不可提交。

# HOST / PORT / DATABASE_PATH 沿用既有默认值（0.0.0.0 / 3000 / dnd.sqlite），无需在此重复。
# 本地开发建议使用独立数据库，避免触碰仓库中的既有 server/dnd.sqlite：
# HOST=127.0.0.1
# DATABASE_PATH=.dev/dnd.sqlite
# Phase 2：普通 server 不再静默创建数据库或 credential key。新实例必须先用离线 CLI 显式初始化：
#   npm run platform:cli -- init --database /abs/path/dnd.sqlite（fresh）
#   npm run platform:cli -- enroll --database /abs/path/dnd.sqlite（existing 001-011）
#   npm run platform:cli -- security-cutover --database /abs/path/dnd.sqlite --backup /abs/path/backup

# Phase 2 请求安全基线（必需）：唯一 canonical HTTPS origin（scheme://host[:port]，无 path/query/hash）。
# 缺失或非法时生产启动 fail closed。
# PUBLIC_ORIGIN=https://dnd.example.com

# 可信反向代理 CIDR（逗号分隔精确 IP/CIDR；默认空）。只有来自这些地址的
# X-Forwarded-* 才会被信任；禁止 `trust proxy: true`。
# TRUSTED_PROXY_CIDRS=10.0.0.0/8,192.168.1.0/24
# 本地开发由 Vite loopback proxy 注入 HTTPS 转发头：
# TRUSTED_PROXY_CIDRS=127.0.0.1/32

# 仅非生产环境使用的显式 Origin allowlist（逗号分隔；禁止 `*`）。
# DEV_ALLOWED_ORIGINS=http://localhost:5173
# 本地开发默认前端地址：
# DEV_ALLOWED_ORIGINS=http://127.0.0.1:5180,http://localhost:5180

# 一次性 bootstrap 模式（三选一：全部缺失/非法即 fail closed；发布前必须移除）。
# BOOTSTRAP_ENABLED=true
# BOOTSTRAP_DATABASE_ID=<platform status 输出的 database ID>
# BOOTSTRAP_SECRET=<43 字符 ASCII base64url，32 bytes>

# Phase 2：credential key 只在离线 CLI 显式创建；启动只校验已存在 key 的 fingerprint 并逐行解密。
# 数据库已有密文时缺失/损坏/错误密钥会令启动在监听端口之前失败（fail-closed），绝不生成替代密钥。

# WebUI Provider API Key 使用本地密钥文件进行 AES-256-GCM 加密。
# 数据库尚无任何加密密文时，首次启动自动创建密钥并复用；一旦存在密文，
# 缺失/损坏/错误密钥会令启动在监听端口之前失败（fail-closed），绝不生成替代密钥。
# 本地密钥文件默认与 DATABASE_PATH 位于同一目录，并已由 .gitignore 忽略。

# AI Provider 环境变量仍作为未配置战役的启动回退；在 Owner WebUI 保存后，
# 该战役立即改用数据库中的加密配置，无需编辑 .env 或重启。
# 只有 openai-compatible 会启用真实 Provider；其它值或缺失时安全回落 UnavailableAiProvider。
# 每次 AI 请求至多发出一次 Provider HTTP 请求，不做自动重试。
# AI_PROVIDER=openai-compatible
# AI_PROVIDER_BASE_URL=https://api.openai.com/v1
# AI_PROVIDER_API_KEY=replace-me
# AI_PROVIDER_MODEL=gpt-4o-mini

# 可选运行时参数（缺省或非法值时使用安全默认值）：
# AI_PROVIDER_TIMEOUT_MS=240000
# AI_PROVIDER_TEMPERATURE=0.7

# 数据库尚无任何加密密文时，Phase 1 的过渡性“启动时自动创建 key”已在 Phase 2 移除；
# 零密文 new DB 必须先经 CLI init/enroll（生成/复用 key），普通启动不会创建 key。
